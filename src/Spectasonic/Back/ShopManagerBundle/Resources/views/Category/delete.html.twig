{% extends "SpectasonicBackGestionContactBundle::layout.html.twig" %}


{% block second_title %}
    {{ 'contact.delete.title_page' | trans }} {{ parent() }}
{% endblock %}

{% block contact_content %}
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">{{ enquiry.name }} {{ enquiry.firstname }}</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <div class="row">
        <div class="col-lg-12">
            <p>
                Are you sure to want to delete this contact Id° {{enquiry.id }} ?
            </p> 

            <form action="{{ path('spectasonic_back_gestion_contact_delete', {'id' : enquiry.id}) }}" method="POST">
                <a href="{{ path('spectasonic_back_gestion_contact_view', {'id' : enquiry.id}) }}" class="btn btn-default">
                    <i class=""></i>
                    Go back
                </a>
                <input type="submit" value="Delete" class="btn btn-danger" />
                {{ form_rest(form) }}
            </form>

        </div>
        <!-- .panel-body -->
    </div>
{% endblock %}